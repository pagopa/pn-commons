version: 0.2
phases:
  build:
    commands:
      - echo "Build"
      - ./mvnw clean install
      - ls target
  post_build:
    commands:
      - echo "Deploy"
      - echo $SETTINGS
artifacts:
  files:
    - 'dist/**'
